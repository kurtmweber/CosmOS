#!/usr/bin/env bash

IMAGE=cosmos/ktool:latest
export UID=$(id -u)
export GID=$(id -g)

docker build -t $IMAGE . && \
docker run --rm \
    --user $UID:$GID \
    --tty \
    --interactive \
    --volume "${PWD}":/work \
    $IMAGE "$@"
